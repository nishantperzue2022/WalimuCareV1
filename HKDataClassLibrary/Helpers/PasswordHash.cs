using System;
using System.Collections.Generic;
using System.Text;

namespace HKDataClassLibrary.Helpers
{
    public class PasswordHash
    {
        //public static string hashPass(string unhashedPassword)
        //{
        //    string cipher = Crypto.HashPassword(unhashedPassword);
        //    //Save hashedPassword somewhere that you can retrieve it again.
        //    //Don't save unhashedPassword! Just let it go.
        //    return cipher;
        //}

        //public static bool CheckPassword(string unhashedPassword, string savedHashedPassword)
        //{

        //    return Crypto.VerifyHashedPassword(savedHashedPassword, unhashedPassword);
        //}
    }
}
